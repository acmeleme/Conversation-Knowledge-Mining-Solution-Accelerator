  3. Producing no output for an extended period does not mean the command failed — call get_terminal_output with id="0a530824-1599-44ce-a772-da5668ff1a90" to continue polling. Only call kill_terminal if the command is genuinely hung and you need to retry with a different approach.
PS C:\Users\rodrigoleme\OneDrive - Microsoft\Documents\AnoFiscal2026\demos\Conversation-Knowledge-Mining-Solution-Accelerator> $ErrorActionPreference='Continue'; Write-Output "[retry-step3] non-interactive build/push/update"; $rg='rg-callcenter-100'; $app='app-callcenter100'; $api='api-callcenter100'; $appRef=az webapp config container show -g $rg -n $app --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; $apiRef=az webapp config container show -g $rg -n $api --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; Write-Output "appRef=$appRef"; Write-Output "apiRef=$apiRef"; $tag='hotfix-'+(Get-Date -Format 'yyyyMMddHHmmss'); $appImg=($appRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $apiImg=($apiRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $acrHost=($appImg.Split('/')[0]); $acrName=$acrHost.Split('.')[0]; $token=az acr login --name $acrName --expose-token --query accessToken -o tsv 2>$null; $ecLogin=$LASTEXITCODE; if($ecLogin -eq 0 -and $token){ $token | docker login $acrHost --username 00000000-0000-0000-0000-000000000000 --password-stdin; $ecDockerLogin=$LASTEXITCODE } else { $ecDockerLogin=1; Write-Output "ACR token login failed for $acrName" }; if($ecDockerLogin -eq 0){ docker build --platform linux/amd64 -f src/App/WebApp.Dockerfile -t $appImg src/App; $ecAppBuild=$LASTEXITCODE; if($ecAppBuild -eq 0){ docker push $appImg; $ecAppPush=$LASTEXITCODE } else { $ecAppPush=1 }; docker build --platform linux/amd64 -f src/api/ApiApp.Dockerfile -t $apiImg src/api; $ecApiBuild=$LASTEXITCODE; if($ecApiBuild -eq 0){ docker push $apiImg; $ecApiPush=$LASTEXITCODE } else { $ecApiPush=1 }; if($ecAppPush -eq 0){ az webapp config container set -g $rg -n $app --container-image-name $appImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecAppSet=$LASTEXITCODE } else { $ecAppSet=1 }; if($ecApiPush -eq 0){ az webapp config container set -g $rg -n $api --container-image-name $apiImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecApiSet=$LASTEXITCODE } else { $ecApiSet=1 }; if($ecAppSet -eq 0){ az webapp restart -g $rg -n $app | Out-Null; $ecAppRestart=$LASTEXITCODE } else { $ecAppRestart=1 }; if($ecApiSet -eq 0){ az webapp restart -g $rg -n $api | Out-Null; $ecApiRestart=$LASTEXITCODE } else { $ecApiRestart=1 } } else { $ecAppBuild=1; $ecAppPush=1; $ecApiBuild=1; $ecApiPush=1; $ecAppSet=1; $ecApiSet=1; $ecAppRestart=1; $ecApiRestart=1 }; Write-Output "appImg=$appImg"; Write-Output "apiImg=$apiImg"; Write-Output "EXIT_CODES=acrToken:$ecLogin dockerLogin:$ecDockerLogin appBuild:$ecAppBuild appPush:$ecAppPush apiBuild:$ecApiBuild apiPush:$ecApiPush appSet:$ecAppSet apiSet:$ecApiSet appRestart:$ecAppRestart apiRestart:$ecApiRestart"; Write-Output "`n[step4] verify chart endpoint"; try { $r
PS C:\Users\rodrigoleme\OneDrive - Microsoft\Documents\AnoFiscal2026\demos\Conversation-Knowledge-Mining-Solution-Accelerator> $ErrorActionPreference='Continue'; Write-Output "[retry-step3] non-interactive build/push/update"; $rg='rg-callcenter-100'; $app='app-callcenter100'; $api='api-callcenter100'; $appRef=az webapp config container show -g $rg -n $app --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; $apiRef=az webapp config container show -g $rg -n $api --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; Write-Output "appRef=$appRef"; Write-Output "apiRef=$apiRef"; $tag='hotfix-'+(Get-Date -Format 'yyyyMMddHHmmss'); $appImg=($appRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $apiImg=($apiRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $acrHost=($appImg.Split('/')[0]); $acrName=$acrHost.Split('.')[0]; $token=az acr login --name $acrName --expose-token --query accessToken -o tsv 2>$null; $ecLogin=$LASTEXITCODE; if($ecLogin -eq 0 -and $token){ $token | docker login $acrHost --username 00000000-0000-0000-0000-000000000000 --password-stdin; $ecDockerLogin=$LASTEXITCODE } else { $ecDockerLogin=1; Write-Output "ACR token login failed for $acrName" }; if($ecDockerLogin -eq 0){ docker build --platform linux/amd64 -f src/App/WebApp.Dockerfile -t $appImg src/App; $ecAppBuild=$LASTEXITCODE; if($ecAppBuild -eq 0){ docker push $appImg; $ecAppPush=$LASTEXITCODE } else { $ecAppPush=1 }; docker build --platform linux/amd64 -f src/api/ApiApp.Dockerfile -t $apiImg src/api; $ecApiBuild=$LASTEXITCODE; if($ecApiBuild -eq 0){ docker push $apiImg; $ecApiPush=$LASTEXITCODE } else { $ecApiPush=1 }; if($ecAppPush -eq 0){ az webapp config container set -g $rg -n $app --container-image-name $appImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecAppSet=$LASTEXITCODE } else { $ecAppSet=1 }; if($ecApiPush -eq 0){ az webapp config container set -g $rg -n $api --container-image-name $apiImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecApiSet=$LASTEXITCODE } else { $ecApiSet=1 }; if($ecAppSet -eq 0){ az webapp restart -g $rg -n $app | Out-Null; $ecAppRestart=$LASTEXITCODE } else { $ecAppRestart=1 }; if($ecApiSet -eq 0){ az webapp restart -g $rg -n $api | Out-Null; $ecApiRestart=$LASTEXITCODE } else { $ecApiRestart=1 } } else { $ecAppBuild=1; $ecAppPush=1; $ecApiBuild=1; $ecApiPush=1; $ecAppSet=1; $ecApiSet=1; $ecAppRestart=1; $ecApiRestart=1 }; Write-Output "appImg=$appImg"; Write-Output "apiImg=$apiImg"; Write-Output "EXIT_CODES=acrToken:$ecLogin dockerLogin:$ecDockerLogin appBuild:$ecAppBuild appPush:$ecAppPush apiBuild:$ecApiBuild apiPush:$ecApiPush appSet:$ecAppSet apiSet:$ecApiSet appRestart:$ecAppRestart apiRestart:$ecApiRestart"; Write-Output "`n[step4] verify chart endpoint"; try { $resp=Invoke-RestMethod -Method Get -Uri 'https://api-callcenter100.azurewebsites.net/api/fetchChartData' -TimeoutSec 120; Write-Output "endpoint_status=SUCCESS"; $resp | ConvertTo-Json -Depth 6 } catch { Write-Output "endpoint_status=ERROR"; Write-Output $_.Exception.
PS C:\Users\rodrigoleme\OneDrive - Microsoft\Documents\AnoFiscal2026\demos\Conversation-Knowledge-Mining-Solution-Accelerator> $ErrorActionPreference='Continue'; Write-Output "[retry-step3] non-interactive build/push/update"; $rg='rg-callcenter-100'; $app='app-callcenter100'; $api='api-callcenter100'; $appRef=az webapp config container show -g $rg -n $app --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; $apiRef=az webapp config container show -g $rg -n $api --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; Write-Output "appRef=$appRef"; Write-Output "apiRef=$apiRef"; $tag='hotfix-'+(Get-Date -Format 'yyyyMMddHHmmss'); $appImg=($appRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $apiImg=($apiRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $acrHost=($appImg.Split('/')[0]); $acrName=$acrHost.Split('.')[0]; $token=az acr login --name $acrName --expose-token --query accessToken -o tsv 2>$null; $ecLogin=$LASTEXITCODE; if($ecLogin -eq 0 -and $token){ $token | docker login $acrHost --username 00000000-0000-0000-0000-000000000000 --password-stdin; $ecDockerLogin=$LASTEXITCODE } else { $ecDockerLogin=1; Write-Output "ACR token login failed for $acrName" }; if($ecDockerLogin -eq 0){ docker build --platform linux/amd64 -f src/App/WebApp.Dockerfile -t $appImg src/App; $ecAppBuild=$LASTEXITCODE; if($ecAppBuild -eq 0){ docker push $appImg; $ecAppPush=$LASTEXITCODE } else { $ecAppPush=1 }; docker build --platform linux/amd64 -f src/api/ApiApp.Dockerfile -t $apiImg src/api; $ecApiBuild=$LASTEXITCODE; if($ecApiBuild -eq 0){ docker push $apiImg; $ecApiPush=$LASTEXITCODE } else { $ecApiPush=1 }; if($ecAppPush -eq 0){ az webapp config container set -g $rg -n $app --container-image-name $appImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecAppSet=$LASTEXITCODE } else { $ecAppSet=1 }; if($ecApiPush -eq 0){ az webapp config container set -g $rg -n $api --container-image-name $apiImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecApiSet=$LASTEXITCODE } else { $ecApiSet=1 }; if($ecAppSet -eq 0){ az webapp restart -g $rg -n $app | Out-Null; $ecAppRestart=$LASTEXITCODE } else { $ecAppRestart=1 }; if($ecApiSet -eq 0){ az webapp restart -g $rg -n $api | Out-Null; $ecApiRestart=$LASTEXITCODE } else { $ecApiRestart=1 } } else { $ecAppBuild=1; $ecAppPush=1; $ecApiBuild=1; $ecApiPush=1; $ecAppSet=1; $ecApiSet=1; $ecAppRestart=1; $ecApiRestart=1 }; Write-Output "appImg=$appImg"; Write-Output "apiImg=$apiImg"; Write-Output "EXIT_CODES=acrToken:$ecLogin dockerLogin:$ecDockerLogin appBuild:$ecAppBuild appPush:$ecAppPush apiBuild:$ecApiBuild apiPush:$ecApiPush appSet:$ecAppSet apiSet:$ecApiSet appRestart:$ecAppRestart apiRestart:$ecApiRestart"; Write-Output "`n[step4] verify chart endpoint"; try { $resp=Invoke-RestMethod -Method Get -Uri 'https://api-callcenter100.azurewebsites.net/api/fetchChartData' -TimeoutSec 120; Write-Output "endpoint_status=SUCCESS"; $resp | ConvertTo-Json -Depth 6 } catch { Write-Output "endpoint_status=ERROR"; Write-Output $_.Exception.M
PS C:\Users\rodrigoleme\OneDrive - Microsoft\Documents\AnoFiscal2026\demos\Conversation-Knowledge-Mining-Solution-Accelerator> $ErrorActionPreference='Continue'; Write-Output "[retry-step3] non-interactive build/push/update"; $rg='rg-callcenter-100'; $app='app-callcenter100'; $api='api-callcenter100'; $appRef=az webapp config container show -g $rg -n $app --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; $apiRef=az webapp config container show -g $rg -n $api --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; Write-Output "appRef=$appRef"; Write-Output "apiRef=$apiRef"; $tag='hotfix-'+(Get-Date -Format 'yyyyMMddHHmmss'); $appImg=($appRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $apiImg=($apiRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $acrHost=($appImg.Split('/')[0]); $acrName=$acrHost.Split('.')[0]; $token=az acr login --name $acrName --expose-token --query accessToken -o tsv 2>$null; $ecLogin=$LASTEXITCODE; if($ecLogin -eq 0 -and $token){ $token | docker login $acrHost --username 00000000-0000-0000-0000-000000000000 --password-stdin; $ecDockerLogin=$LASTEXITCODE } else { $ecDockerLogin=1; Write-Output "ACR token login failed for $acrName" }; if($ecDockerLogin -eq 0){ docker build --platform linux/amd64 -f src/App/WebApp.Dockerfile -t $appImg src/App; $ecAppBuild=$LASTEXITCODE; if($ecAppBuild -eq 0){ docker push $appImg; $ecAppPush=$LASTEXITCODE } else { $ecAppPush=1 }; docker build --platform linux/amd64 -f src/api/ApiApp.Dockerfile -t $apiImg src/api; $ecApiBuild=$LASTEXITCODE; if($ecApiBuild -eq 0){ docker push $apiImg; $ecApiPush=$LASTEXITCODE } else { $ecApiPush=1 }; if($ecAppPush -eq 0){ az webapp config container set -g $rg -n $app --container-image-name $appImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecAppSet=$LASTEXITCODE } else { $ecAppSet=1 }; if($ecApiPush -eq 0){ az webapp config container set -g $rg -n $api --container-image-name $apiImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecApiSet=$LASTEXITCODE } else { $ecApiSet=1 }; if($ecAppSet -eq 0){ az webapp restart -g $rg -n $app | Out-Null; $ecAppRestart=$LASTEXITCODE } else { $ecAppRestart=1 }; if($ecApiSet -eq 0){ az webapp restart -g $rg -n $api | Out-Null; $ecApiRestart=$LASTEXITCODE } else { $ecApiRestart=1 } } else { $ecAppBuild=1; $ecAppPush=1; $ecApiBuild=1; $ecApiPush=1; $ecAppSet=1; $ecApiSet=1; $ecAppRestart=1; $ecApiRestart=1 }; Write-Output "appImg=$appImg"; Write-Output "apiImg=$apiImg"; Write-Output "EXIT_CODES=acrToken:$ecLogin dockerLogin:$ecDockerLogin appBuild:$ecAppBuild appPush:$ecAppPush apiBuild:$ecApiBuild apiPush:$ecApiPush appSet:$ecAppSet apiSet:$ecApiSet appRestart:$ecAppRestart apiRestart:$ecApiRestart"; Write-Output "`n[step4] verify chart endpoint"; try { $resp=Invoke-RestMethod -Method Get -Uri 'https://api-callcenter100.azurewebsites.net/api/fetchChartData' -TimeoutSec 120; Write-Output "endpoint_status=SUCCESS"; $resp | ConvertTo-Json -Depth 6 } catch { Write-Output "endpoint_status=ERROR"; Write-Output $_.Exception.Me
PS C:\Users\rodrigoleme\OneDrive - Microsoft\Documents\AnoFiscal2026\demos\Conversation-Knowledge-Mining-Solution-Accelerator> $ErrorActionPreference='Continue'; Write-Output "[retry-step3] non-interactive build/push/update"; $rg='rg-callcenter-100'; $app='app-callcenter100'; $api='api-callcenter100'; $appRef=az webapp config container show -g $rg -n $app --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; $apiRef=az webapp config container show -g $rg -n $api --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; Write-Output "appRef=$appRef"; Write-Output "apiRef=$apiRef"; $tag='hotfix-'+(Get-Date -Format 'yyyyMMddHHmmss'); $appImg=($appRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $apiImg=($apiRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $acrHost=($appImg.Split('/')[0]); $acrName=$acrHost.Split('.')[0]; $token=az acr login --name $acrName --expose-token --query accessToken -o tsv 2>$null; $ecLogin=$LASTEXITCODE; if($ecLogin -eq 0 -and $token){ $token | docker login $acrHost --username 00000000-0000-0000-0000-000000000000 --password-stdin; $ecDockerLogin=$LASTEXITCODE } else { $ecDockerLogin=1; Write-Output "ACR token login failed for $acrName" }; if($ecDockerLogin -eq 0){ docker build --platform linux/amd64 -f src/App/WebApp.Dockerfile -t $appImg src/App; $ecAppBuild=$LASTEXITCODE; if($ecAppBuild -eq 0){ docker push $appImg; $ecAppPush=$LASTEXITCODE } else { $ecAppPush=1 }; docker build --platform linux/amd64 -f src/api/ApiApp.Dockerfile -t $apiImg src/api; $ecApiBuild=$LASTEXITCODE; if($ecApiBuild -eq 0){ docker push $apiImg; $ecApiPush=$LASTEXITCODE } else { $ecApiPush=1 }; if($ecAppPush -eq 0){ az webapp config container set -g $rg -n $app --container-image-name $appImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecAppSet=$LASTEXITCODE } else { $ecAppSet=1 }; if($ecApiPush -eq 0){ az webapp config container set -g $rg -n $api --container-image-name $apiImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecApiSet=$LASTEXITCODE } else { $ecApiSet=1 }; if($ecAppSet -eq 0){ az webapp restart -g $rg -n $app | Out-Null; $ecAppRestart=$LASTEXITCODE } else { $ecAppRestart=1 }; if($ecApiSet -eq 0){ az webapp restart -g $rg -n $api | Out-Null; $ecApiRestart=$LASTEXITCODE } else { $ecApiRestart=1 } } else { $ecAppBuild=1; $ecAppPush=1; $ecApiBuild=1; $ecApiPush=1; $ecAppSet=1; $ecApiSet=1; $ecAppRestart=1; $ecApiRestart=1 }; Write-Output "appImg=$appImg"; Write-Output "apiImg=$apiImg"; Write-Output "EXIT_CODES=acrToken:$ecLogin dockerLogin:$ecDockerLogin appBuild:$ecAppBuild appPush:$ecAppPush apiBuild:$ecApiBuild apiPush:$ecApiPush appSet:$ecAppSet apiSet:$ecApiSet appRestart:$ecAppRestart apiRestart:$ecApiRestart"; Write-Output "`n[step4] verify chart endpoint"; try { $resp=Invoke-RestMethod -Method Get -Uri 'https://api-callcenter100.azurewebsites.net/api/fetchChartData' -TimeoutSec 120; Write-Output "endpoint_status=SUCCESS"; $resp | ConvertTo-Json -Depth 6 } catch { Write-Output "endpoint_status=ERROR"; Write-Output $_.Exception.Mes
PS C:\Users\rodrigoleme\OneDrive - Microsoft\Documents\AnoFiscal2026\demos\Conversation-Knowledge-Mining-Solution-Accelerator> $ErrorActionPreference='Continue'; Write-Output "[retry-step3] non-interactive build/push/update"; $rg='rg-callcenter-100'; $app='app-callcenter100'; $api='api-callcenter100'; $appRef=az webapp config container show -g $rg -n $app --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; $apiRef=az webapp config container show -g $rg -n $api --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; Write-Output "appRef=$appRef"; Write-Output "apiRef=$apiRef"; $tag='hotfix-'+(Get-Date -Format 'yyyyMMddHHmmss'); $appImg=($appRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $apiImg=($apiRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $acrHost=($appImg.Split('/')[0]); $acrName=$acrHost.Split('.')[0]; $token=az acr login --name $acrName --expose-token --query accessToken -o tsv 2>$null; $ecLogin=$LASTEXITCODE; if($ecLogin -eq 0 -and $token){ $token | docker login $acrHost --username 00000000-0000-0000-0000-000000000000 --password-stdin; $ecDockerLogin=$LASTEXITCODE } else { $ecDockerLogin=1; Write-Output "ACR token login failed for $acrName" }; if($ecDockerLogin -eq 0){ docker build --platform linux/amd64 -f src/App/WebApp.Dockerfile -t $appImg src/App; $ecAppBuild=$LASTEXITCODE; if($ecAppBuild -eq 0){ docker push $appImg; $ecAppPush=$LASTEXITCODE } else { $ecAppPush=1 }; docker build --platform linux/amd64 -f src/api/ApiApp.Dockerfile -t $apiImg src/api; $ecApiBuild=$LASTEXITCODE; if($ecApiBuild -eq 0){ docker push $apiImg; $ecApiPush=$LASTEXITCODE } else { $ecApiPush=1 }; if($ecAppPush -eq 0){ az webapp config container set -g $rg -n $app --container-image-name $appImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecAppSet=$LASTEXITCODE } else { $ecAppSet=1 }; if($ecApiPush -eq 0){ az webapp config container set -g $rg -n $api --container-image-name $apiImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecApiSet=$LASTEXITCODE } else { $ecApiSet=1 }; if($ecAppSet -eq 0){ az webapp restart -g $rg -n $app | Out-Null; $ecAppRestart=$LASTEXITCODE } else { $ecAppRestart=1 }; if($ecApiSet -eq 0){ az webapp restart -g $rg -n $api | Out-Null; $ecApiRestart=$LASTEXITCODE } else { $ecApiRestart=1 } } else { $ecAppBuild=1; $ecAppPush=1; $ecApiBuild=1; $ecApiPush=1; $ecAppSet=1; $ecApiSet=1; $ecAppRestart=1; $ecApiRestart=1 }; Write-Output "appImg=$appImg"; Write-Output "apiImg=$apiImg"; Write-Output "EXIT_CODES=acrToken:$ecLogin dockerLogin:$ecDockerLogin appBuild:$ecAppBuild appPush:$ecAppPush apiBuild:$ecApiBuild apiPush:$ecApiPush appSet:$ecAppSet apiSet:$ecApiSet appRestart:$ecAppRestart apiRestart:$ecApiRestart"; Write-Output "`n[step4] verify chart endpoint"; try { $resp=Invoke-RestMethod -Method Get -Uri 'https://api-callcenter100.azurewebsites.net/api/fetchChartData' -TimeoutSec 120; Write-Output "endpoint_status=SUCCESS"; $resp | ConvertTo-Json -Depth 6 } catch { Write-Output "endpoint_status=ERROR"; Write-Output $_.Exception.Mess
PS C:\Users\rodrigoleme\OneDrive - Microsoft\Documents\AnoFiscal2026\demos\Conversation-Knowledge-Mining-Solution-Accelerator> $ErrorActionPreference='Continue'; Write-Output "[retry-step3] non-interactive build/push/update"; $rg='rg-callcenter-100'; $app='app-callcenter100'; $api='api-callcenter100'; $appRef=az webapp config container show -g $rg -n $app --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; $apiRef=az webapp config container show -g $rg -n $api --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; Write-Output "appRef=$appRef"; Write-Output "apiRef=$apiRef"; $tag='hotfix-'+(Get-Date -Format 'yyyyMMddHHmmss'); $appImg=($appRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $apiImg=($apiRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $acrHost=($appImg.Split('/')[0]); $acrName=$acrHost.Split('.')[0]; $token=az acr login --name $acrName --expose-token --query accessToken -o tsv 2>$null; $ecLogin=$LASTEXITCODE; if($ecLogin -eq 0 -and $token){ $token | docker login $acrHost --username 00000000-0000-0000-0000-000000000000 --password-stdin; $ecDockerLogin=$LASTEXITCODE } else { $ecDockerLogin=1; Write-Output "ACR token login failed for $acrName" }; if($ecDockerLogin -eq 0){ docker build --platform linux/amd64 -f src/App/WebApp.Dockerfile -t $appImg src/App; $ecAppBuild=$LASTEXITCODE; if($ecAppBuild -eq 0){ docker push $appImg; $ecAppPush=$LASTEXITCODE } else { $ecAppPush=1 }; docker build --platform linux/amd64 -f src/api/ApiApp.Dockerfile -t $apiImg src/api; $ecApiBuild=$LASTEXITCODE; if($ecApiBuild -eq 0){ docker push $apiImg; $ecApiPush=$LASTEXITCODE } else { $ecApiPush=1 }; if($ecAppPush -eq 0){ az webapp config container set -g $rg -n $app --container-image-name $appImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecAppSet=$LASTEXITCODE } else { $ecAppSet=1 }; if($ecApiPush -eq 0){ az webapp config container set -g $rg -n $api --container-image-name $apiImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecApiSet=$LASTEXITCODE } else { $ecApiSet=1 }; if($ecAppSet -eq 0){ az webapp restart -g $rg -n $app | Out-Null; $ecAppRestart=$LASTEXITCODE } else { $ecAppRestart=1 }; if($ecApiSet -eq 0){ az webapp restart -g $rg -n $api | Out-Null; $ecApiRestart=$LASTEXITCODE } else { $ecApiRestart=1 } } else { $ecAppBuild=1; $ecAppPush=1; $ecApiBuild=1; $ecApiPush=1; $ecAppSet=1; $ecApiSet=1; $ecAppRestart=1; $ecApiRestart=1 }; Write-Output "appImg=$appImg"; Write-Output "apiImg=$apiImg"; Write-Output "EXIT_CODES=acrToken:$ecLogin dockerLogin:$ecDockerLogin appBuild:$ecAppBuild appPush:$ecAppPush apiBuild:$ecApiBuild apiPush:$ecApiPush appSet:$ecAppSet apiSet:$ecApiSet appRestart:$ecAppRestart apiRestart:$ecApiRestart"; Write-Output "`n[step4] verify chart endpoint"; try { $resp=Invoke-RestMethod -Method Get -Uri 'https://api-callcenter100.azurewebsites.net/api/fetchChartData' -TimeoutSec 120; Write-Output "endpoint_status=SUCCESS"; $resp | ConvertTo-Json -Depth 6 } catch { Write-Output "endpoint_status=ERROR"; Write-Output $_.Exception.Messa
PS C:\Users\rodrigoleme\OneDrive - Microsoft\Documents\AnoFiscal2026\demos\Conversation-Knowledge-Mining-Solution-Accelerator> $ErrorActionPreference='Continue'; Write-Output "[retry-step3] non-interactive build/push/update"; $rg='rg-callcenter-100'; $app='app-callcenter100'; $api='api-callcenter100'; $appRef=az webapp config container show -g $rg -n $app --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; $apiRef=az webapp config container show -g $rg -n $api --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; Write-Output "appRef=$appRef"; Write-Output "apiRef=$apiRef"; $tag='hotfix-'+(Get-Date -Format 'yyyyMMddHHmmss'); $appImg=($appRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $apiImg=($apiRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $acrHost=($appImg.Split('/')[0]); $acrName=$acrHost.Split('.')[0]; $token=az acr login --name $acrName --expose-token --query accessToken -o tsv 2>$null; $ecLogin=$LASTEXITCODE; if($ecLogin -eq 0 -and $token){ $token | docker login $acrHost --username 00000000-0000-0000-0000-000000000000 --password-stdin; $ecDockerLogin=$LASTEXITCODE } else { $ecDockerLogin=1; Write-Output "ACR token login failed for $acrName" }; if($ecDockerLogin -eq 0){ docker build --platform linux/amd64 -f src/App/WebApp.Dockerfile -t $appImg src/App; $ecAppBuild=$LASTEXITCODE; if($ecAppBuild -eq 0){ docker push $appImg; $ecAppPush=$LASTEXITCODE } else { $ecAppPush=1 }; docker build --platform linux/amd64 -f src/api/ApiApp.Dockerfile -t $apiImg src/api; $ecApiBuild=$LASTEXITCODE; if($ecApiBuild -eq 0){ docker push $apiImg; $ecApiPush=$LASTEXITCODE } else { $ecApiPush=1 }; if($ecAppPush -eq 0){ az webapp config container set -g $rg -n $app --container-image-name $appImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecAppSet=$LASTEXITCODE } else { $ecAppSet=1 }; if($ecApiPush -eq 0){ az webapp config container set -g $rg -n $api --container-image-name $apiImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecApiSet=$LASTEXITCODE } else { $ecApiSet=1 }; if($ecAppSet -eq 0){ az webapp restart -g $rg -n $app | Out-Null; $ecAppRestart=$LASTEXITCODE } else { $ecAppRestart=1 }; if($ecApiSet -eq 0){ az webapp restart -g $rg -n $api | Out-Null; $ecApiRestart=$LASTEXITCODE } else { $ecApiRestart=1 } } else { $ecAppBuild=1; $ecAppPush=1; $ecApiBuild=1; $ecApiPush=1; $ecAppSet=1; $ecApiSet=1; $ecAppRestart=1; $ecApiRestart=1 }; Write-Output "appImg=$appImg"; Write-Output "apiImg=$apiImg"; Write-Output "EXIT_CODES=acrToken:$ecLogin dockerLogin:$ecDockerLogin appBuild:$ecAppBuild appPush:$ecAppPush apiBuild:$ecApiBuild apiPush:$ecApiPush appSet:$ecAppSet apiSet:$ecApiSet appRestart:$ecAppRestart apiRestart:$ecApiRestart"; Write-Output "`n[step4] verify chart endpoint"; try { $resp=Invoke-RestMethod -Method Get -Uri 'https://api-callcenter100.azurewebsites.net/api/fetchChartData' -TimeoutSec 120; Write-Output "endpoint_status=SUCCESS"; $resp | ConvertTo-Json -Depth 6 } catch { Write-Output "endpoint_status=ERROR"; Write-Output $_.Exception.Messag
PS C:\Users\rodrigoleme\OneDrive - Microsoft\Documents\AnoFiscal2026\demos\Conversation-Knowledge-Mining-Solution-Accelerator> $ErrorActionPreference='Continue'; Write-Output "[retry-step3] non-interactive build/push/update"; $rg='rg-callcenter-100'; $app='app-callcenter100'; $api='api-callcenter100'; $appRef=az webapp config container show -g $rg -n $app --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; $apiRef=az webapp config container show -g $rg -n $api --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; Write-Output "appRef=$appRef"; Write-Output "apiRef=$apiRef"; $tag='hotfix-'+(Get-Date -Format 'yyyyMMddHHmmss'); $appImg=($appRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $apiImg=($apiRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $acrHost=($appImg.Split('/')[0]); $acrName=$acrHost.Split('.')[0]; $token=az acr login --name $acrName --expose-token --query accessToken -o tsv 2>$null; $ecLogin=$LASTEXITCODE; if($ecLogin -eq 0 -and $token){ $token | docker login $acrHost --username 00000000-0000-0000-0000-000000000000 --password-stdin; $ecDockerLogin=$LASTEXITCODE } else { $ecDockerLogin=1; Write-Output "ACR token login failed for $acrName" }; if($ecDockerLogin -eq 0){ docker build --platform linux/amd64 -f src/App/WebApp.Dockerfile -t $appImg src/App; $ecAppBuild=$LASTEXITCODE; if($ecAppBuild -eq 0){ docker push $appImg; $ecAppPush=$LASTEXITCODE } else { $ecAppPush=1 }; docker build --platform linux/amd64 -f src/api/ApiApp.Dockerfile -t $apiImg src/api; $ecApiBuild=$LASTEXITCODE; if($ecApiBuild -eq 0){ docker push $apiImg; $ecApiPush=$LASTEXITCODE } else { $ecApiPush=1 }; if($ecAppPush -eq 0){ az webapp config container set -g $rg -n $app --container-image-name $appImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecAppSet=$LASTEXITCODE } else { $ecAppSet=1 }; if($ecApiPush -eq 0){ az webapp config container set -g $rg -n $api --container-image-name $apiImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecApiSet=$LASTEXITCODE } else { $ecApiSet=1 }; if($ecAppSet -eq 0){ az webapp restart -g $rg -n $app | Out-Null; $ecAppRestart=$LASTEXITCODE } else { $ecAppRestart=1 }; if($ecApiSet -eq 0){ az webapp restart -g $rg -n $api | Out-Null; $ecApiRestart=$LASTEXITCODE } else { $ecApiRestart=1 } } else { $ecAppBuild=1; $ecAppPush=1; $ecApiBuild=1; $ecApiPush=1; $ecAppSet=1; $ecApiSet=1; $ecAppRestart=1; $ecApiRestart=1 }; Write-Output "appImg=$appImg"; Write-Output "apiImg=$apiImg"; Write-Output "EXIT_CODES=acrToken:$ecLogin dockerLogin:$ecDockerLogin appBuild:$ecAppBuild appPush:$ecAppPush apiBuild:$ecApiBuild apiPush:$ecApiPush appSet:$ecAppSet apiSet:$ecApiSet appRestart:$ecAppRestart apiRestart:$ecApiRestart"; Write-Output "`n[step4] verify chart endpoint"; try { $resp=Invoke-RestMethod -Method Get -Uri 'https://api-callcenter100.azurewebsites.net/api/fetchChartData' -TimeoutSec 120; Write-Output "endpoint_status=SUCCESS"; $resp | ConvertTo-Json -Depth 6 } catch { Write-Output "endpoint_status=ERROR"; Write-Output $_.Exception.Message
PS C:\Users\rodrigoleme\OneDrive - Microsoft\Documents\AnoFiscal2026\demos\Conversation-Knowledge-Mining-Solution-Accelerator> $ErrorActionPreference='Continue'; Write-Output "[retry-step3] non-interactive build/push/update"; $rg='rg-callcenter-100'; $app='app-callcenter100'; $api='api-callcenter100'; $appRef=az webapp config container show -g $rg -n $app --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; $apiRef=az webapp config container show -g $rg -n $api --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; Write-Output "appRef=$appRef"; Write-Output "apiRef=$apiRef"; $tag='hotfix-'+(Get-Date -Format 'yyyyMMddHHmmss'); $appImg=($appRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $apiImg=($apiRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $acrHost=($appImg.Split('/')[0]); $acrName=$acrHost.Split('.')[0]; $token=az acr login --name $acrName --expose-token --query accessToken -o tsv 2>$null; $ecLogin=$LASTEXITCODE; if($ecLogin -eq 0 -and $token){ $token | docker login $acrHost --username 00000000-0000-0000-0000-000000000000 --password-stdin; $ecDockerLogin=$LASTEXITCODE } else { $ecDockerLogin=1; Write-Output "ACR token login failed for $acrName" }; if($ecDockerLogin -eq 0){ docker build --platform linux/amd64 -f src/App/WebApp.Dockerfile -t $appImg src/App; $ecAppBuild=$LASTEXITCODE; if($ecAppBuild -eq 0){ docker push $appImg; $ecAppPush=$LASTEXITCODE } else { $ecAppPush=1 }; docker build --platform linux/amd64 -f src/api/ApiApp.Dockerfile -t $apiImg src/api; $ecApiBuild=$LASTEXITCODE; if($ecApiBuild -eq 0){ docker push $apiImg; $ecApiPush=$LASTEXITCODE } else { $ecApiPush=1 }; if($ecAppPush -eq 0){ az webapp config container set -g $rg -n $app --container-image-name $appImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecAppSet=$LASTEXITCODE } else { $ecAppSet=1 }; if($ecApiPush -eq 0){ az webapp config container set -g $rg -n $api --container-image-name $apiImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecApiSet=$LASTEXITCODE } else { $ecApiSet=1 }; if($ecAppSet -eq 0){ az webapp restart -g $rg -n $app | Out-Null; $ecAppRestart=$LASTEXITCODE } else { $ecAppRestart=1 }; if($ecApiSet -eq 0){ az webapp restart -g $rg -n $api | Out-Null; $ecApiRestart=$LASTEXITCODE } else { $ecApiRestart=1 } } else { $ecAppBuild=1; $ecAppPush=1; $ecApiBuild=1; $ecApiPush=1; $ecAppSet=1; $ecApiSet=1; $ecAppRestart=1; $ecApiRestart=1 }; Write-Output "appImg=$appImg"; Write-Output "apiImg=$apiImg"; Write-Output "EXIT_CODES=acrToken:$ecLogin dockerLogin:$ecDockerLogin appBuild:$ecAppBuild appPush:$ecAppPush apiBuild:$ecApiBuild apiPush:$ecApiPush appSet:$ecAppSet apiSet:$ecApiSet appRestart:$ecAppRestart apiRestart:$ecApiRestart"; Write-Output "`n[step4] verify chart endpoint"; try { $resp=Invoke-RestMethod -Method Get -Uri 'https://api-callcenter100.azurewebsites.net/api/fetchChartData' -TimeoutSec 120; Write-Output "endpoint_status=SUCCESS"; $resp | ConvertTo-Json -Depth 6 } catch { Write-Output "endpoint_status=ERROR"; Write-Output $_.Exception.Message 
PS C:\Users\rodrigoleme\OneDrive - Microsoft\Documents\AnoFiscal2026\demos\Conversation-Knowledge-Mining-Solution-Accelerator> $ErrorActionPreference='Continue'; Write-Output "[retry-step3] non-interactive build/push/update"; $rg='rg-callcenter-100'; $app='app-callcenter100'; $api='api-callcenter100'; $appRef=az webapp config container show -g $rg -n $app --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; $apiRef=az webapp config container show -g $rg -n $api --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]" -o tsv; Write-Output "appRef=$appRef"; Write-Output "apiRef=$apiRef"; $tag='hotfix-'+(Get-Date -Format 'yyyyMMddHHmmss'); $appImg=($appRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $apiImg=($apiRef -replace '^DOCKER\|','') -replace ':.*$',":$tag"; $acrHost=($appImg.Split('/')[0]); $acrName=$acrHost.Split('.')[0]; $token=az acr login --name $acrName --expose-token --query accessToken -o tsv 2>$null; $ecLogin=$LASTEXITCODE; if($ecLogin -eq 0 -and $token){ $token | docker login $acrHost --username 00000000-0000-0000-0000-000000000000 --password-stdin; $ecDockerLogin=$LASTEXITCODE } else { $ecDockerLogin=1; Write-Output "ACR token login failed for $acrName" }; if($ecDockerLogin -eq 0){ docker build --platform linux/amd64 -f src/App/WebApp.Dockerfile -t $appImg src/App; $ecAppBuild=$LASTEXITCODE; if($ecAppBuild -eq 0){ docker push $appImg; $ecAppPush=$LASTEXITCODE } else { $ecAppPush=1 }; docker build --platform linux/amd64 -f src/api/ApiApp.Dockerfile -t $apiImg src/api; $ecApiBuild=$LASTEXITCODE; if($ecApiBuild -eq 0){ docker push $apiImg; $ecApiPush=$LASTEXITCODE } else { $ecApiPush=1 }; if($ecAppPush -eq 0){ az webapp config container set -g $rg -n $app --container-image-name $appImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecAppSet=$LASTEXITCODE } else { $ecAppSet=1 }; if($ecApiPush -eq 0){ az webapp config container set -g $rg -n $api --container-image-name $apiImg --container-registry-url ("https://"+$acrHost) | Out-Null; $ecApiSet=$LASTEXITCODE } else { $ecApiSet=1 }; if($ecAppSet -eq 0){ az webapp restart -g $rg -n $app | Out-Null; $ecAppRestart=$LASTEXITCODE } else { $ecAppRestart=1 }; if($ecApiSet -eq 0){ az webapp restart -g $rg -n $api | Out-Null; $ecApiRestart=$LASTEXITCODE } else { $ecApiRestart=1 } } else { $ecAppBuild=1; $ecAppPush=1; $ecApiBuild=1; $ecApiPush=1; $ecAppSet=1; $ecApiSet=1; $ecAppRestart=1; $ecApiRestart=1 }; Write-Output "appImg=$appImg"; Write-Output "apiImg=$apiImg"; Write-Output "EXIT_CODES=acrToken:$ecLogin dockerLogin:$ecDockerLogin appBuild:$ecAppBuild appPush:$ecAppPush apiBuild:$ecApiBuild apiPush:$ecApiPush appSet:$ecAppSet apiSet:$ecApiSet appRestart:$ecAppRestart apiRestart:$ecApiRestart"; Write-Output "`n[step4] verify chart endpoint"; try { $resp=Invoke-RestMethod -Method Get -Uri 'https://api-callcenter100.azurewebsites.net/api/fetchChartData' -TimeoutSec 120; Write-Output "endpoint_status=SUCCESS"; $resp | ConvertTo-Json -Depth 6 } catch { Write-Output "endpoint_status=ERROR"; Write-Output $_.Exception.Message }
