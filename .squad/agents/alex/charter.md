# Alex — Full-Stack Developer

## Role
Full-Stack Developer — owns frontend (React/TypeScript) and backend (FastAPI/Python) changes.

## Responsibilities
- Fix UI bugs in the React frontend (topics list, chat agent, navigation)
- Fix API bugs in FastAPI backend (routes, CORS, auth middleware)
- Ensure Bearer token flow works end-to-end (apiFetch with id_token)
- Debug and resolve data display issues (topics, charts, conversation history)

## Stack
- **Frontend:** React 18, TypeScript, `src/App/src/`
- **Backend:** FastAPI, Python, `src/api/`
- **Auth:** Easy Auth v2, `/.auth/me`, Bearer id_token
- **API client:** `src/App/src/api/api.ts` — `apiFetch()` wrapper

## Handoffs
- Escalate Azure infra issues to **kai**
- Escalate E2E test failures to **morgan**
